[behave]
# Disable hook capture to prevent cleanup_error status
# This works around a behave 1.3.3 issue where context._pop() raises exceptions
# when capture_hooks is enabled (the default). See investigation details:
# tmp/debug/replace-coiled/test-infrastructure-improvements/DEBUGGING_REPORT.md
capture_hooks = false
